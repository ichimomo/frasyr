% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/post_assess.R
\name{find_project_root}
\alias{find_project_root}
\title{Find a project root by walking up parent directories}
\usage{
find_project_root(
  path = getwd(),
  markers = "future_outputs",
  winslash = "/",
  mustWork = TRUE
)
}
\arguments{
\item{path}{Character scalar. Directory to start searching from. Defaults to
the current working directory.}

\item{markers}{Character vector. File or directory names that must exist
directly under the project root.}

\item{winslash}{Character scalar passed to [normalizePath()] on Windows.}

\item{mustWork}{Logical. If `TRUE`, throw an error when no matching parent
directory is found. If `FALSE`, return `NA_character_`.}
}
\value{
A normalized path to the first parent directory containing all
  `markers`, or `NA_character_` when `mustWork = FALSE` and no root is found.
}
\description{
This helper starts from a directory and walks upward until it finds a
directory that contains all requested marker files or directories. It is used
by post-assessment scripts to locate a project root without depending on a
fixed working directory.
}
\examples{
\dontrun{
find_project_root(markers = "future_outputs")
}
}
