% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/post_assess.R
\name{as_age_year_tibble}
\alias{as_age_year_tibble}
\title{Convert an age-year matrix to a long tibble}
\usage{
as_age_year_tibble(mat, stat = NULL, age_names = NULL, year_names = NULL)
}
\arguments{
\item{mat}{Matrix-like object. Rows are ages and columns are years.}

\item{stat}{Optional character scalar added as a `stat` column.}

\item{age_names}{Optional age labels. Defaults to `rownames(mat)`, or
zero-based row indices when row names are missing.}

\item{year_names}{Optional year labels. Defaults to `colnames(mat)`, or
one-based column indices when column names are missing.}
}
\value{
A tibble with columns `age`, `year`, `value`, and optionally `stat`.
}
\description{
This helper converts a matrix whose rows are ages and columns are years into
a long table with `age`, `year`, and `value` columns. It is useful for
post-assessment summaries of age-specific quantities such as numbers at age,
fishing mortality, weight, and maturity.
}
